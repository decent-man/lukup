ALGO:-

1. Site-Setup
  a. Open the target website with Inspector.
  b. Have the user select the parent element which corresponds to the text he is interested in.
  c. Save all underlying object model from the selected element into a file. -- Called as skeleton.
  d. Analyze the the whole object model below and build a dataclass to represent all text(maybe images).
  e. Save this and all built model into files under the website's directory to be accesed by the master program.

2. Master
  a. Get Search term from user.
  b. Print a menu selection to Ask for website to search for.
  c. Load the skel and dataclass from file(s) for the website chosen.
  d. Get the stuff.
  e. Print formatted output.
